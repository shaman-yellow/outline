
- 设置方法，允许在 method, snap 内部对图片的引用。可以要关注到 `sig` 和具体基因的干扰。需要模糊匹配。
- 增加修改 tag 的功能，在对应的 R 文件内部的 Legend 后增加 tag，然后增添 R 函数用于检索这些 tag
- 在 R script 中增加使用的方法的tag，以R 函数检索这些tag，最后自动 collate 到 Rmd 中。
- 转化方案的docx 为 markdown 文件，然后考虑是否以读取的形式来嵌入文字……
- …………
- 琐碎事项，标题，目录，结尾的文件夹所在备注，
- counting save，
- R script 的备份，
- 备份之后的发送代码，自动保存？——这个应该放在报告完成以后。
- …………
- 拟时分析是否可以现在进行了？


